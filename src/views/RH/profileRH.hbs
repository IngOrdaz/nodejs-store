{{>navbarRH}}
<div class="container ">
        <div class="row  justify-content-center align-items-center ">
            
                <div class="col-10 col-md-8 col-lg-6 shadow-lg p-3 mb-5 mt-4  rounded" style="background-color: #61947F;"> 
                    <form action="/rh/edit-employee/{{employ._id}}?_method=PUT" class="mt4 needs-validation" method="POST" novalidate>
                        <input type="hidden" name="_method" value="PUT">
                        <h1 id="h1_register_employee">Datos del Empleado {{employ.name}}</h1>
                         <br>
                        <div class="form-group mx-auto centro"  >
                            <label for="name">Nombre</label>
                            <input id="name" name="name" type="text" class="form-control"  value= "{{employ.name}}" placeholder="Nombre" autofocus required disabled>
                        </div>
                        
                        <div class="form-group mx-auto centro">
                            <label for="fatherLastName">Apellido Paterno</label>
                            <input id="fatherLastName" name="fatherLastName" type="text" class="form-control" placeholder="Apellido Paterno" value="{{employ.fatherLastName}}" required disabled>

                        </div>
                        
                        <div class="form-group mx-auto centro">
                            <label for="motherLastName">Apellido Materno</label>
                            <input id="motherLastName" name="motherLastName" type="text" class="form-control" placeholder="Apellido Materno" value="{{employ.motherLastName}}" disabled required>
                        </div>
                        
                        <!--TODO BIRTHDATE-->
                       
                        <div class="form-group mx-auto centro">
                            <label for="curp">CURP</label>
                            <input id="curp" name="curp" type="text" class="form-control" placeholder="CURP" value="{{employ.curp}}" required disabled>
                        </div>
                        
                        <div class="form-group mx-auto centro">
                            <label for="rfc">RFC</label>
                            <input id="rfc" name="rfc" type="text" class="form-control" placeholder="RFC" value="{{employ.rfc}}" required disabled>
                        </div>
                        
                        <div class="form-group mx-auto centro">
                            <label for="email">Correo</label>
                            <input id="email" name="email" type="email" class="form-control" placeholder="Email" value="{{employ.email}}" required disabled>
                        </div>
                     
                        <div class="form-group mx-auto centro">
                            <label for="phone">Teléfono</label>
                            <input id="phone" name="phone" type="text" class="form-control" placeholder="Teléfono" value="{{employ.phone}}" required disabled>
                        </div>
                        <!-- Start Date -->

                        <div class="form-group mx-auto centro">
                            <label for="job">Puesto</label>
                            <select name="job" id="job" class="form-select" required disabled>
                                <option selected disabled>{{employ.job}}</option>
                            </select>
                        </div>
                        
                        <div class="form-group mx-auto centro">
                            <label for="rol">Rol de Usuario</label>
                            <select name="rol" id="rol" class="form-select" required disabled>
                                <option disabled selected value="">{{employ.rol}}</option>
                            </select>
                        </div>
                        <div class="form-group mx-auto centro">
                            <label for="salary">Salario</label>
                            <input id="salary" name="salary" type="text" class="form-control" placeholder="Salario Quincenal" value="{{employ.salary}}" required disabled>
                        </div>    
                        <!-- TODO workinhourstart -->
                        
                        <!-- TODO workinhourend -->
    
                        <!-- TODO photo -->
                        
                        <div class="form-group mx-auto centro">
                            <label for="user">Usuario</label>
                            <input id="user" name="user" type="text" class="form-control" placeholder="Usuario" value="{{employ.user}}" disabled>
                        </div>    
                    

                        <a href="" class="btn btn-success"> Imprimir Datos</a>
                        <a href="" class="btn btn-warning"> Cambiar Contraseña</a>
                    </form>
                </div>
            
        </div>    
    </div>
    
    <!-- script para validación de datos-->
    <script>
        // Example starter JavaScript for disabling form submissions if there are invalid fields
    (function () {
      'use strict'
    
      // Fetch all the forms we want to apply custom Bootstrap validation styles to
      var forms = document.querySelectorAll('.needs-validation')
    
      // Loop over them and prevent submission
      Array.prototype.slice.call(forms)
        .forEach(function (form) {
          form.addEventListener('submit', function (event) {
            if (!form.checkValidity()) {
              event.preventDefault()
              event.stopPropagation()
            }
    
            form.classList.add('was-validated')
          }, false)
        })
    })()
    </script>
    	<!-- Javascript -->
	 

</body>
</html>